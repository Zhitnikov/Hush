# Обычно не нужен: /api идёт на тот же host (Vite preview / nginx → :5000).
# НЕ ставьте http://localhost:5000 — сломает loca.lt (CORS).
# Только если API на другом домене в production:
# VITE_API_ORIGIN=https://api.example.com
